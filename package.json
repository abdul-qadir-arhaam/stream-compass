{
  "name": "stream-compass-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "install:all": "npm install --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  }
}
